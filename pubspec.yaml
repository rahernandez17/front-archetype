name: front_archetype
description: Aplicación para obtener certificados generados por la Institución Universitaria Antonio José Camacho.

version: 1.0.0+1

environment:
  sdk: '>=2.1.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^0.1.2
  http: ^0.12.1
  firebase_messaging: ^6.0.16

dev_dependencies:
  flutter_test:
    sdk: flutter
  test_api: ^0.2.11
  mockito: ^4.1.1
  test_coverage: ^0.4.1

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:
  uses-material-design: true
  # To add assets to your application, add an assets section, like this:
  # assets:
  #  - images/a_dot_burr.jpeg
  #  - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware.

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  fonts:
    - family: Roboto
      fonts:
        - asset: lib/src/resource/fonts/Roboto/Roboto-Light.ttf
        - asset: lib/src/resource/fonts/Roboto/Roboto-Regular.ttf
          style: normal
    - family: Raleway
      fonts:
        - asset: lib/src/resource/fonts/Raleway/Raleway-Light.ttf
        - asset: lib/src/resource/fonts/Raleway/Raleway-Regular.ttf
          weight: 400
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
